<Window x:Class="Toreno.AddUsernameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Username" Height="220" Width="380"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ADD USERNAME" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="USERNAME" Style="{StaticResource CaptionStyle}" Margin="2,0,0,4" />
            <TextBox x:Name="UsernameTextBox" />
        </StackPanel>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="2" Foreground="{StaticResource DangerBrush}"
                    FontSize="11" TextWrapping="Wrap" Visibility="Collapsed" Margin="2,8,0,0" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL" IsCancel="True" Click="CancelButton_OnClick" />
            <Button Content="ADD" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0"
                    IsDefault="True" Click="AddButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
