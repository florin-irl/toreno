<Window x:Class="Toreno.AddServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Server" Height="300" Width="420"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ADD SERVER" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="SERVER ADDRESS" Style="{StaticResource CaptionStyle}" Margin="2,0,0,4" />
            <TextBox x:Name="AddressTextBox" Text="127.0.0.1:7777" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="DISPLAY NAME (OPTIONAL)" Style="{StaticResource CaptionStyle}" Margin="2,0,0,4" />
            <TextBox x:Name="NameTextBox" />
        </StackPanel>

        <TextBlock x:Name="ErrorTextBlock" Grid.Row="3" Foreground="{StaticResource DangerBrush}"
                    FontSize="11" TextWrapping="Wrap" Visibility="Collapsed" Margin="2,4,0,0" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CANCEL" IsCancel="True" Click="CancelButton_OnClick" />
            <Button Content="ADD SERVER" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0"
                    IsDefault="True" Click="AddButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
