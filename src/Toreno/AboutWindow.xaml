<Window x:Class="Toreno.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Toreno" Height="300" Width="380"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="TORENO" FontFamily="Bahnschrift SemiBold" FontSize="26"
                   FontWeight="Bold" Margin="0,0,0,16" />

        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,20"
                   Text="Watches a SA-MP server's player list and fires a native Windows notification the instant someone you're watching for connects." />

        <StackPanel Grid.Row="2" VerticalAlignment="Top">
            <TextBlock Text="CONTACT" Style="{StaticResource CaptionStyle}" Margin="0,0,0,4" />
            <TextBlock FontSize="13">
                <Hyperlink NavigateUri="https://github.com/florin-irl" RequestNavigate="Hyperlink_OnRequestNavigate">
                    github.com/florin-irl
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="CLOSE" Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True" IsCancel="True" Click="CloseButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
